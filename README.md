
  # Onboard2Earn

  Staff-assisted customer onboarding and referral rewards app. Field staff capture customer KYC, generate login credentials, and track their rank/points/tier on a leaderboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # AccessbankOnboardToEarn
